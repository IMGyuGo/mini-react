// ============================================================
// contentBlocks.js — 교육용 UI 블록 모음
// ============================================================
//
// react.dev처럼 학습 콘텐츠를 예쁘고 구조적으로 보여주기 위한
// 재사용 가능한 UI 블록들을 모아놓은 파일.
//
// 사용할 수 있는 블록 종류:
//
//   ┌─ YouWillLearn ─────────────────────────┐
//   │ 📚 이번 섹션에서 배울 것들               │
//   │  • 컴포넌트란 무엇인지                   │
//   │  • 컴포넌트를 만드는 방법                 │
//   └────────────────────────────────────────┘
//
//   ┌─ Note ─────────────────────────────────┐
//   │ 💡 참고: Hook은 최상위에서만 사용 가능!   │
//   └────────────────────────────────────────┘
//
//   ┌─ DeepDive ─────────────────────────────┐
//   │ 🔍 더 알아보기: 실제 React와의 차이       │
//   │ (클릭하면 펼쳐지는 아코디언)              │
//   └────────────────────────────────────────┘
//
//   ┌─ Challenge ────────────────────────────┐
//   │ 🏆 도전 과제 1: 프로필 카드 만들기        │
//   │ [힌트 보기]  [정답 보기]                 │
//   └────────────────────────────────────────┘
// ============================================================

// ------------------------------------------------------------
// createYouWillLearn(items)
// ------------------------------------------------------------
// "이번 섹션에서 배울 것들" 목록 박스를 만든다.
// 각 섹션의 맨 위에 놓여서 학습 목표를 먼저 보여준다.
//
// [매개변수]
//   items — 학습 목표 문자열 배열
//           예: ['컴포넌트의 개념', '컴포넌트를 만드는 방법']
//
// [반환값]
//   DOM 요소 (바로 appendChild 가능)
//
// [사용 예시]
//   const box = createYouWillLearn([
//     '컴포넌트가 무엇인지 이해한다',
//     '함수형 컴포넌트를 직접 만들어본다',
//   ]);
//   section.appendChild(box);
// ------------------------------------------------------------
export function createYouWillLearn(items) {
  // TODO: 구현하기

  // <div class="you-will-learn">
  //   <h3>이번 섹션에서 배울 것들</h3>
  //   <ul>
  //     <li>컴포넌트가 무엇인지 이해한다</li>
  //     <li>함수형 컴포넌트를 직접 만들어본다</li>
  //   </ul>
  // </div>
}

// ------------------------------------------------------------
// createNote(content)
// ------------------------------------------------------------
// 중요한 참고 사항을 강조하는 노란색 박스.
// "이것만은 꼭 기억하세요!" 같은 정보를 넣는다.
//
// [매개변수]
//   content — 문자열 또는 DOM 요소
//
// [반환값]
//   DOM 요소
// ------------------------------------------------------------
export function createNote(content) {
  // TODO: 구현하기

  // <div class="note-block">
  //   <span class="note-icon">💡</span>
  //   <div class="note-content">내용</div>
  // </div>
}

// ------------------------------------------------------------
// createDeepDive(title, content)
// ------------------------------------------------------------
// "더 알아보기" 아코디언 블록.
// 기본적으로 접혀 있고, 클릭하면 펼쳐진다.
// 심화 내용을 궁금한 사람만 볼 수 있게 한다.
//
// [매개변수]
//   title   — 아코디언 헤더에 표시할 제목
//   content — 펼쳤을 때 보여줄 내용 (문자열 또는 DOM 요소)
//
// [반환값]
//   DOM 요소
//
// [구현 힌트]
//   HTML의 <details>와 <summary> 태그를 쓰면
//   자바스크립트 없이도 접기/펼치기가 가능하다!
// ------------------------------------------------------------
export function createDeepDive(title, content) {
  // TODO: 구현하기

  // <details class="deep-dive">
  //   <summary>🔍 더 알아보기: {title}</summary>
  //   <div class="deep-dive-content">{content}</div>
  // </details>
}

// ------------------------------------------------------------
// createChallenge(options)
// ------------------------------------------------------------
// 학습자가 직접 풀어보는 도전 과제 블록.
//
// [매개변수]
//   options = {
//     number      — 과제 번호 (예: 1)
//     title       — 과제 제목 (예: "프로필 카드 만들기")
//     description — 과제 설명 텍스트
//     hint        — 힌트 텍스트 (선택)
//     solution    — 정답 코드 문자열 (선택)
//   }
//
// [반환값]
//   DOM 요소
//
// [동작]
//   - "힌트 보기" 버튼 → 힌트 텍스트 토글
//   - "정답 보기" 버튼 → 정답 코드 토글
//   - 처음에는 힌트와 정답이 숨겨져 있다
//     (먼저 스스로 풀어보도록 유도)
// ------------------------------------------------------------
export function createChallenge(options = {}) {
  // TODO: 구현하기

  // <div class="challenge-block">
  //   <h4>🏆 도전 {number}: {title}</h4>
  //   <p>{description}</p>
  //   <button class="hint-btn">힌트 보기</button>
  //   <div class="hint-content" hidden>{hint}</div>
  //   <button class="solution-btn">정답 보기</button>
  //   <div class="solution-content" hidden>{solution}</div>
  // </div>
}

// ------------------------------------------------------------
// createSectionHeader(title, subtitle)
// ------------------------------------------------------------
// 각 섹션의 제목과 부제목을 만든다.
//
// [매개변수]
//   title    — 대제목 (예: "Component란?")
//   subtitle — 부제목 (예: "UI를 나누는 가장 작은 단위")
// ------------------------------------------------------------
export function createSectionHeader(title, subtitle) {
  // TODO: 구현하기

  // <div class="section-header">
  //   <h2>{title}</h2>
  //   <p class="section-subtitle">{subtitle}</p>
  // </div>
}

// ------------------------------------------------------------
// createParagraph(text)
// ------------------------------------------------------------
// 본문 텍스트 단락을 만든다.
// 마크다운의 `코드` 표시를 <code> 태그로 변환한다.
//
// [매개변수]
//   text — 본문 텍스트. 백틱(`)으로 감싼 부분은 코드로 표시.
//
// [예시]
//   createParagraph('`useState`는 상태를 관리하는 hook입니다.')
//   → <p><code>useState</code>는 상태를 관리하는 hook입니다.</p>
// ------------------------------------------------------------
export function createParagraph(text) {
  // TODO: 구현하기
}

// ------------------------------------------------------------
// createCodeBlock(code, language)
// ------------------------------------------------------------
// 읽기 전용 코드 블록을 만든다. (실행 불가, 보여주기용)
//
// [매개변수]
//   code     — 코드 문자열
//   language — 언어 (예: 'js', 'html') — 스타일링용
//
// [반환값]
//   DOM 요소 (<pre><code>...</code></pre>)
// ------------------------------------------------------------
export function createCodeBlock(code, language = 'js') {
  // TODO: 구현하기

  // <pre class="code-block" data-language="{language}">
  //   <code>{code}</code>
  // </pre>
}

// ------------------------------------------------------------
// createRecap(items)
// ------------------------------------------------------------
// 섹션 끝에 넣는 "요약 정리" 박스.
//
// [매개변수]
//   items — 요약 포인트 문자열 배열
// ------------------------------------------------------------
export function createRecap(items) {
  // TODO: 구현하기

  // <div class="recap-block">
  //   <h3>📝 요약</h3>
  //   <ul>
  //     <li>...</li>
  //   </ul>
  // </div>
}
